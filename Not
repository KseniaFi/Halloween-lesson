<!DOCTYPE html>
<html lang="en">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Spooky Halloween Experience</title>
    <style>
        * {
            margin: 0;
            padding: 0;
            box-sizing: border-box;
        }
        
        body {
            font-family: 'Arial', sans-serif;
            background: url('1000054415.jpg') no-repeat center center fixed;
            background-size: cover;
            color: #fff;
            overflow: hidden;
            position: relative;
            height: 100vh;
        }
        
        .overlay {
            position: absolute;
            top: 0;
            left: 0;
            width: 100%;
            height: 100%;
            background: linear-gradient(to bottom, rgba(0,0,0,0.2) 0%, rgba(0,0,0,0.7) 70%);
            z-index: 1;
        }
        
        .content {
            position: relative;
            z-index: 2;
            padding: 20px;
            height: 100vh;
            display: flex;
            flex-direction: column;
            justify-content: space-between;
        }
        
        header {
            text-align: center;
            padding-top: 30px;
        }
        
        h1 {
            font-size: 3.5rem;
            color: #ff7518;
            text-shadow: 3px 3px 6px rgba(0, 0, 0, 0.7);
            margin-bottom: 10px;
            letter-spacing: 2px;
        }
        
        .subtitle {
            font-size: 1.5rem;
            color: #ffcc00;
            text-shadow: 2px 2px 4px rgba(0, 0, 0, 0.7);
        }
        
        .subtitles-container {
            position: absolute;
            bottom: 120px;
            left: 5%;
            width: 90%;
            background-color: rgba(0, 0, 0, 0.7);
            padding: 20px;
            border-radius: 15px;
            border: 2px solid #ff7518;
            box-shadow: 0 0 20px rgba(255, 117, 24, 0.5);
            min-height: 120px;
            display: flex;
            align-items: center;
        }
        
        #subtitles {
            font-size: 1.4rem;
            line-height: 1.6;
            text-align: center;
            width: 100%;
        }
        
        .word {
            display: inline-block;
            margin-right: 5px;
            opacity: 0.7;
            transition: opacity 0.3s, color 0.3s;
        }
        
        .word.highlight {
            opacity: 1;
            color: #ffcc00;
            text-shadow: 0 0 8px rgba(255, 204, 0, 0.8);
        }
        
        .controls {
            position: absolute;
            bottom: 30px;
            left: 50%;
            transform: translateX(-50%);
            display: flex;
            gap: 20px;
        }
        
        .btn {
            background-color: #ff7518;
            color: #fff;
            border: none;
            padding: 12px 25px;
            border-radius: 30px;
            font-size: 1.1rem;
            cursor: pointer;
            transition: all 0.3s;
            box-shadow: 0 4px 8px rgba(0, 0, 0, 0.3);
        }
        
        .btn:hover {
            background-color: #ff8c42;
            transform: translateY(-2px);
            box-shadow: 0 6px 12px rgba(0, 0, 0, 0.4);
        }
        
        .pumpkin {
            position: absolute;
            bottom: 40px;
            right: 40px;
            font-size: 4rem;
            filter: drop-shadow(0 0 10px rgba(255, 117, 24, 0.7));
            animation: float 3s ease-in-out infinite;
        }
        
        .bat {
            position: absolute;
            top: 80px;
            right: 80px;
            font-size: 3rem;
            animation: fly 8s linear infinite;
        }
        
        @keyframes float {
            0%, 100% { transform: translateY(0); }
            50% { transform: translateY(-15px); }
        }
        
        @keyframes fly {
            0% { transform: translateX(0) translateY(0); }
            25% { transform: translateX(-50px) translateY(20px); }
            50% { transform: translateX(-100px) translateY(0); }
            75% { transform: translateX(-50px) translateY(-20px); }
            100% { transform: translateX(0) translateY(0); }
        }
        
        @media (max-width: 768px) {
            h1 {
                font-size: 2.5rem;
            }
            
            .subtitle {
                font-size: 1.2rem;
            }
            
            #subtitles {
                font-size: 1.2rem;
            }
            
            .subtitles-container {
                bottom: 100px;
                padding: 15px;
            }
            
            .pumpkin, .bat {
                display: none;
            }
        }
    </style>
</head>
<body>
    <div class="overlay"></div>
    
    <div class="content">
        <header>
            <h1>Happy Halloween!</h1>
            <div class="subtitle">Spooky Halloween Experience</div>
        </header>
        
        <div class="subtitles-container">
            <div id="subtitles"></div>
        </div>
        
        <div class="controls">
            <button class="btn" id="startBtn">Start Story</button>
            <button class="btn" id="pauseBtn">Pause</button>
        </div>
        
        <div class="pumpkin">🎃</div>
        <div class="bat">🦇</div>
    </div>

    <script>
        document.addEventListener('DOMContentLoaded', function() {
            const subtitles = document.getElementById('subtitles');
            const startBtn = document.getElementById('startBtn');
            const pauseBtn = document.getElementById('pauseBtn');
            
            // Текст для отображения (разбит на предложения)
            const sentences = [
                "The phrase means that the children are asking for a treat, usually candy, and if they don't get one, they might play a small trick on the homeowner.",
                "This custom is a fun part of Halloween celebrations when kids collect sweets from their neighbours."
            ];
            
            let currentSentenceIndex = 0;
            let currentWordIndex = 0;
            let isPlaying = false;
            let wordInterval;
            let sentenceTimeout;
            
            // Функция для отображения слов по одному
            function displayWords() {
                if (!isPlaying) return;
                
                const sentence = sentences[currentSentenceIndex];
                const words = sentence.split(' ');
                
                // Очищаем субтитры
                subtitles.innerHTML = '';
                
                // Создаем элементы для каждого слова
                words.forEach((word, index) => {
                    const wordElement = document.createElement('span');
                    wordElement.className = 'word';
                    wordElement.textContent = word + ' ';
                    subtitles.appendChild(wordElement);
                });
                
                // Подсвечиваем слова по одному
                const wordElements = subtitles.querySelectorAll('.word');
                
                function highlightNextWord() {
                    if (!isPlaying) return;
                    
                    // Убираем подсветку с предыдущего слова
                    if (currentWordIndex > 0) {
                        wordElements[currentWordIndex - 1].classList.remove('highlight');
                    }
                    
                    // Подсвечиваем текущее слово
                    if (currentWordIndex < wordElements.length) {
                        wordElements[currentWordIndex].classList.add('highlight');
                        currentWordIndex++;
                        
                        // Запускаем следующий таймер
                        wordInterval = setTimeout(highlightNextWord, 150);
                    } else {
                        // Все слова в предложении показаны
                        currentWordIndex = 0;
                        
                        // Переходим к следующему предложению через 2 секунды
                        sentenceTimeout = setTimeout(() => {
                            currentSentenceIndex++;
                            if (currentSentenceIndex < sentences.length) {
                                displayWords();
                            } else {
                                // Все предложения показаны
                                isPlaying = false;
                                startBtn.textContent = 'Restart Story';
                            }
                        }, 2000);
                    }
                }
                
                // Запускаем подсветку слов
                highlightNextWord();
            }
            
            // Обработчики кнопок
            startBtn.addEventListener('click', function() {
                if (!isPlaying) {
                    isPlaying = true;
                    startBtn.textContent = 'Playing...';
                    
                    // Если это рестарт, сбрасываем индексы
                    if (currentSentenceIndex >= sentences.length) {
                        currentSentenceIndex = 0;
                        currentWordIndex = 0;
                    }
                    
                    displayWords();
                }
            });
            
            pauseBtn.addEventListener('click', function() {
                if (isPlaying) {
                    isPlaying = false;
                    startBtn.textContent = 'Resume';
                    clearTimeout(wordInterval);
                    clearTimeout(sentenceTimeout);
                } else {
                    isPlaying = true;
                    startBtn.textContent = 'Playing...';
                    displayWords();
                }
            });
        });
    </script>
</body>
</html>
